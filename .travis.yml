language: node_js

#if you want to specify a particular node.js version
node_js:
  - 14 

before_install:
- pyenv install 3.6.0

install:
- npm install
- cd src/backend/
- pip3 install -r requirements.txt

script:

